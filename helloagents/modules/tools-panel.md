# tools-panel

## 职责

提供 DevTools 面板容器：左侧导航、路由切换与统一布局，让各工具以页面形式集成。

## 行为规范

### 面板导航生成
**条件**: tools.html 加载
**行为**: 左侧导航菜单从 `utils/tool-modules.ts` 的注册表生成；主内容区按路由渲染对应工具组件
**结果**: 面板菜单与 Popup 菜单默认一致（同一注册表）

## 依赖关系

```yaml
依赖:
  - tool-modules
被依赖: []
```

